import React from "react";
import recentStyles from '../Recent/index.module.scss';

const Recent = ({title, date, url, img}) => {
    return (
        <div className={recentStyles.container}>
            <div className={recentStyles.featuredCard}>
                
            </div>
        </div>
    )
}

export default Recent